accessWidener v2 official

# No access wideners are currently required.
#
# In 26.1 the ghost-chicken disguise no longer mutates vanilla packet fields or
# walks ChunkMap.TrackedEntity directly. EntityTrackerEntryMixin injects into
# ServerEntity#sendPairingData and constructs a fresh ClientboundAddEntityPacket,
# and the tab-list handling uses an @Accessor mixin — neither needs widened access.
